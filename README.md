# Asp.Net Core + React + TypeScript + Webpack POC
POC to use React + TypeScript + WebPack with ASP.NET Core

Steps:
- Clone the project
- Open the sln with Visual Studio, all necessary packages should be automatically restored
- Hit F5
